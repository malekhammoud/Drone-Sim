gnureadline
billiard
future
numpy
pyparsing
MAVProxy
# setuptools<81 is required on Python 3.14: MAVProxy imports pkg_resources,
# which was removed from setuptools 81+.
setuptools<81

# arcticlib runtime. opencv headless: we only decode/encode, never open a window.
requests
opencv-python-headless
# Optional: only needed to load config.yaml instead of env vars.
PyYAML
